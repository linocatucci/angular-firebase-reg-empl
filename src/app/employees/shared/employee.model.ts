export class Employee {

  $key: string; // key will be used it will create
  name: string;
  position: string;
  office: string;
  salary: number;

  constructor() {

    /*
    Todo: Constructor in the class is used to construct / create a new instance of the class/ new object. Add the properties in the constructor
*/
    /*
   constructor($key: string, name: string, position: string, office: string, salary:number) {
     this.$key = $key;
     this.name = name;
     this.position = position;
     this.office = office;
   }
   */

  }
}
